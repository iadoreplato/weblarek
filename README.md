# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

#### Данные
В приложении используется несколько сущностей, которые описывают данные.
##### Интерфейс данных IProduct 
Интерфейс содержит описание товара, получаемое от сервера.

  interface IProduct {
  id: string; - id товара,
  description: string; - описание товара,
  image: string; - изображение товара,
  title: string; - название товара,
  category: string; - категория товара,
  price: number | null; - стоимость товара.
}

##### Интерфейс данных IBuyer
Интерфейс содержит описание покупателя, передаваемое на сервер.

interface IBuyer {
  payment: TPayment; -способ оплаты, 'card'|'cash'|''|undefined;
  email: string; - имейл покупателя,
  phone: string; - номер телефона покупателя,
  address: string; - адрес доставки.
} 
##### Интерфейс данных IProductsList
Интерфейс содержит описание всех товаров, получаемое от сервера.

interface IProductsList {
  total: number; - общее количество товаров;
  items: IProduct[]; - массив всех товаров
  }

##### Интерфейс данных IOrderRequest
Интерфейс содержит описание данных заказа, передаваемых на сервер. 
Расширяет интерфейс IBuyer, добавляя сумму заказа и список id товаров.

interface IOrderRequest extends IBuyer {
  total: number; - сумма заказа,
  items: string[]; - массив id выбранных товаров.
}

##### Интерфейс данных IOrderResponse
Интерфейс содержит описание ответа сервера при оформлении заказа.

interface IOrderResponse {
  id: string; - id созданного заказа,
  total: number; - сумма заказа.
}

#### Модели данных
##### Класс Catalogue
Хранит список товаров каталога и управляет выбранным товаром.

Конструктор:  
`constructor(events: IEvents)` - В конструктор передается брокер событий.

Поля класса:  
`protected _products: IProduct[]` - защищенный массив  всех товаров в виде интерфейса IProduct,
`protected _productCard: IProduct | null` - защищенная выбранная карточка товара в виде интерфейса IProduct или null, если ни один товар не выбран.
`protected _events: IEvents` - защищенный экземпляр брокера событий.

Методы:  
`setProducts(products: IProduct[]): void` - сохраняет массив товаров, полученный в параметрах метода;
`getAllProducts(): IProduct[]` - не принимает параметры и получает список всех товаров в виде массива.
`getProduct(id: string): IProduct` - принимает id товара и получает информацию о выбранной карточке - объект в виде IProduct
`setProductCard(productCard: IProduct): void` - принимает объект с данными и сохраняет товар для подробного отображения;
`getProductCard(): IProduct | null` - не принимает параметры и получает товар для подробного отображения; возвращает null, если ни один товар не выбран.

##### Класс Cart
Содержит в себе список товаров, добавленных в корзину и управляет ими.

Конструктор:  
`constructor(events: IEvents)` - В конструктор передается брокер событий.

Поля класса:  
`protected _selectedProducts: IProduct[]` - защищенный массив товаров, добавленных в корзину, в виде интерфейса IProduct
`protected _events: IEvents` - защищенный экземпляр брокера событий.

Методы:  
`getSelectedProducts(): IProduct[]` - не принимает параметры и получает список выбранных товаров в виде массива.
`addProduct(product: IProduct):  void` - принимает объект с данными и добавляет его к массиву.
`deleteProduct(product: IProduct): void` - принимает объект с данными и удаляет его из массива.
`clearCart(): void` - не принимает параметры  и очищает корзину.
`getTotalNumber(): number` - не принимает параметры и возвращает количество товаров в корзине.
`getTotalPrice(): number` - не принимает параметры и возвращает сумму всех товаров в корзине.
`isAvailable(id: string): boolean` - принимает id товара и возвращает true, если товар уже добавлен в корзину.

##### Класс Buyer
Содержит в себе информацию о данных покупателя и управляет ей. 

Конструктор:  
`constructor(events: IEvents)` - В конструктор передается брокер событий.

Поля класса:  
`protected _buyer: IBuyer` - защищенный объект с данными о покупателе в виде интерфейса IBuyer.
`protected _events: IEvents` - защищенный экземпляр брокера событий.

Методы:  
`dataValidation(data: Partial<IBuyer>): Partial<Record<keyof IBuyer, string>>` - валидирует данные покупателя
`getBuyerData(): IBuyer` - не принимает параметры и возвращает все данные покупателя.
`clearBuyerData(): void` - не принимает параметры  и очищает данные покупателя.
`setPurchaseData(data: Partial<IBuyer>): void` - принимает объект с одним или несколькими полями покупателя и сохраняет их, не затирая остальные поля.

#### Слой коммуникации
##### Класс shopApi
Отвечает за взаимодействие с сервером магазина. Использует экземпляр класса Api для выполнения запросов.

Конструктор:
`constructor(api: IApi)` - принимает объект, реализующий интерфейс IApi.

Поля класса:  
`protected _api: IApi` - объект для выполнения запросов к серверу.

Методы:  
`getProducts(): Promise<IProduct[]>` - выполняет GET запрос на эндпоинт `/product/`, получает объект типа IProductList и возвращает массив товаров из поля items.
`createOrder(order: IOrderRequest): Promise<IOrderResponse>` - выполняет post запрос на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода.

#### Слой представления
##### Интерфейс ProductCardData
Описывает данные, передаваемые в метод render карточки товара.

interface ProductCardData {
  title: string; - название товара,
  price: number | null; - стоимость товара,
  buttonState?: { text: string, disabled: boolean } - состояние кнопки
}

##### Класс ProductCard 
Абстрактный базовый класс для всех карточек товара. Содержит общие элементы и методы, которые используются во всех вариантах карточек.

Конструктор:
`constructor(container: HTMLElement)` - принимает DOM-элемент клонированного темплейта, вызывает конструктор Component и находит дочерние элементы разметки.


Поля класса:  
`protected titleElement: HTMLElement` - защищенный HTML-элемент, содержащий название товара
`protected priceElement: HTMLElement` - защищенный HTML-элемент, содержащий  стоимость товара

Методы:  
`set title (value: string)` - устанавливает текстовое содержимое titleElement
`set price(value: number | null)` - устанавливает текстовое содержимое priceElement

##### Интерфейс CatalogueItemData 
Описывает данные, передаваемые в метод render карточки товара в каталоге товаров.
interface CatalogueItemData {
category: string - категория товара
image: string - изображение товара
}

##### Класс CatalogueItem
Дочерний класс карточки товара в каталоге. Содержит  элементы и методы, которые используются в карточках товара в каталоге товаров на главной странице.

Конструктор:
`constructor(container: HTMLElement, actions: { onClick: () => void })` - принимает DOM-элемент клонированного темплейта и объект с колбэком onClick, который вызывается при клике на карточку.

Поля класса:  
`protected categoryElement: HTMLElement` - защищенный HTML-элемент, содержащий категорию товара
`protected imageElement: HTMLImageElement` - защищенный HTML-элемент, содержащий  изображение товара

Методы:  
`set category(value: string)` - устанавливает текстовое содержимое categoryElement
`set image (value: string)` - устанавливает атрибут src элемента imageElement

##### Интерфейс CardPreviewData
Описывает данные, передаваемые в метод render карточки товара в модальном окне Preview. 
interface CardPreviewData {
category: string - категория товара
description: string, - описание товара
image: string - изображение товара
}

##### Класс CardPreview
Дочерний класс карточки товара в каталоге. Содержит  элементы и методы, которые используются в карточках товара в каталоге товаров в модальном окне Preview

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` - принимает DOM-элемент и брокер событий.

Поля класса:  
`protected categoryElement: HTMLElement` - защищенный HTML-элемент, содержащий категорию товара.
`protected descriptionElement: HTMLElement` - защищенный HTML-элемент, содержащий описание товара.
`protected imageElement: HTMLImageElement` - защищенный HTML-элемент, содержащий изображение товара.
`protected buttonElement: HTMLButtonElement` - защищенный HTML-элемент, содержащий кнопку действия.

Методы:  
`set category(value: string)` - устанавливает текстовое содержимое categoryElement.
`set description(value: string)` - устанавливает текстовое содержимое descriptionElement.
`set image(value: string)` - устанавливает атрибут src элемента imageElement.
`set buttonState(value: { text: string, disabled: boolean })` - устанавливает текст и состояние кнопки.


##### Интерфейс BasketProductCardData
Описывает данные, передаваемые в метод render карточки товара в корзине. 

interface BasketProductCardData {
counter: number - порядковый номер товара в корзине
}

##### Класс BasketProductCard
Дочерний класс карточки товара в каталоге. Содержит  элементы и методы, которые используются в карточках товара в каталоге товаров в в корзине. 

Конструктор:
`constructor(container: HTMLElement, actions: { onClick: () => void })` - принимает DOM-элемент клонированного темплейта и объект с колбэком onClick, который вызывается при клике на кнопку удаления

Поля класса:  
`protected counterElement: HTMLElement` - защищенный HTML-элемент, содержащий порядковый номер твоара в корзине
`protected trashBinElement: HTMLButtonElement` - защищенный HTML-элемент, содержащий кнопку удаления товара из корзины

Методы:  
`set counter (value: number)` - устанавливает текстовое содержимое counterElement


##### Интерфейс  CartData
Описывает данные, передаваемые в метод render карточки товара в корзине. 
interface CartData {
content: HTMLElement[] - массив карточек товаров 
totalAmount: number - общая стоимость товаров в корзине
}

##### Класс CartView
Класс корзины с товарами. Содержит  элементы и методы, которые используются в корзине. 

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` - принимает брокер событий и DOM-элемент клонированного темплейта, вызывает конструктор Component и находит дочерние элементы разметки.

Поля класса:  
`protected productList: HTMLElement` - защищенный HTML-элемент, содержащий список товаров в корзине
`protected totalAmountElement: HTMLElement` - защищенный HTML-элемент, содержащий общую стоимость товаров в корзине
`protected orderButton: HTMLButtonElement` - кнопка оформления заказа

Методы:  
`set content (items: HTMLElement[])` - устанавливает содержимое productList
`set totalAmount (value: number)` - устанавливает содержимое totalAmountElement

##### Интерфейс FormData
Описывает данные, передаваемые в метод render формы.

interface FormData {
  valid: boolean; - валидность введенных данных
  error: string; - сообщение об ошибке
}

##### Класс Form
Абстрактный базовый класс для всех форм. Содержит общие элементы и методы, которые используются во всех вариантах форм.

Конструктор:
`constructor(container: HTMLElement, events: IEvents, submitEvent: string)` - принимает DOM-элемент клонированного темплейта, брокер событий и название события которое будет генерироваться при отправке формы.

Поля класса:  
`protected submitButton: HTMLButtonElement` - защищенный HTML-элемент, содержащий кнопку "Далее"
`protected errorField: HTMLElement` - защищенный HTML-элемент, содержащий поле вывода ошибки

Методы:  
`set valid (value: boolean)` - убирает/устанавливает свойство disabled у submitButton
`set error (value: string)` - устанавливает текстовое содержимое errorField

##### Интерфейс OrderData
Описывает данные, передаваемые в метод render формы

interface OrderData extends FormData {
  addressInput : string - адрес доставки
  paymentMethod: TPayment - способ оплаты 
}

##### Класс OrderForm
Дочерний класс Form для формы с данными о способе оплаты и адресе доставки заказа. Содержит общие элементы и методы, которые используются в форме ввода способа оплаты и адреса доставки заказа.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` - принимает брокер событий и DOM-элемент клонированного темплейта, вызывает конструктор Component и находит дочерние элементы разметки.

Поля класса:  
`protected addressInputElement: HTMLInputElement` - защищенный HTML-элемент, содержащий адрес доставки
`protected cardButtonElement: HTMLButtonElement` - защищенный HTML-элемент, содержащий способ оплаты 'карта'
`protected cashButtonElement: HTMLButtonElement` - защищенный HTML-элемент, содержащий способ оплаты 'наличные'

Методы:  
`set paymentMethod (value: TPayment)` - устанавливает модификатор button_alt-active на выбранную кнопку оплаты - cardButtonElement или cashButtonElement
`set addressInput(value: string)` - устанавливает текстовое содержимое addressInputElement

##### Интерфейс ContactData
Описывает данные, передаваемые в метод render формы

interface ContactData extends FormData {
  email: string - имейл покупателя
  phone: string - номер телефона покупателя
}

##### Класс ContactForm
Дочерний класс Form для формы с данными об имейле и номере телефона покупателя. Содержит общие элементы и методы, которые используются в форме ввода имейла и номера телефона покупателя

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` - принимает брокер событий и DOM-элемент клонированного темплейта, вызывает конструктор Component и находит дочерние элементы разметки.

Поля класса:  
`protected emailInputElement: HTMLInputElement` - защищенный HTML-элемент, содержащий имейл покупателя
`protected phoneInputElement: HTMLInputElement` - защищенный HTML-элемент, содержащий номер телефона покупателя

Методы:  
`set email (value: string)` - устанавливает текстовое содержимое emailInputElement
`set phone (value: string)` - устанавливает текстовое содержимое phoneInputElement

##### Интерфейс ModalData
Описывает данные, передаваемые в метод render модального окна

interface ModalData {
  content: HTMLElement - базовое содержимое модального окна (кнопка закрытия и контент модального окна)
}

##### Класс Modal
Базовый класс модального окна. Содержит общие элементы и методы, которые используются в модальном окне

Конструктор:
`constructor(container: HTMLElement)` - принимает существующий DOM-элемент 
модального окна из разметки страницы.

Поля класса:  
`protected closeButton: HTMLButtonElement` - защищенный HTML-элемент, содержащий кнопку закрытия модельного окна
`protected modalContent: HTMLElement` - защищенный HTML-элемент, содержащий конкретный контент модального окна

Методы:  
`set content (item: HTMLElement)` - устанавливает содержимое modalContent
`open(): void` - открывает модальное окно добавляя модификатор modal_active
`close(): void` - закрывает модальное окно убирая модификатор modal_active

##### Интерфейс OrderSuccessData
Описывает данные, передаваемые в метод render модального окна

interface OrderSuccessData {
  totalAmount: number - общая стоимость покупки
}

##### Класс OrderSuccess
Класс окна с подтверждением заказа. Содержит общие элементы и методы, которые используются в окне подтверждения заказа

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` - принимает брокер событий и DOM-элемент клонированного темплейта, вызывает конструктор Component и находит дочерние элементы разметки.

Поля класса:  
`protected amountElement: HTMLElement` - защищенный HTML-элемент, содержащий общую стоимость покупки
`protected nextButton: HTMLButtonElement` - защищенный HTML-элемент, содержащий кнопку перехода в каталог товаров

Методы:  
`set totalAmount (value: number)` - устанавливает содержимое amountElement

##### Интерфейс HeaderData
Описывает данные, передаваемые в метод render шапки главной страницы

interface HeaderData {
 counter: number - общее количество товаров в корзине
}

##### Класс Header
Класс шапки главной страницы. Содержит общие элементы и методы, которые используются в шапке главной страницы

Конструктор:
`constructor(container: HTMLElement, events: IEvents) ` - принимает брокер событий и DOM-элемент клонированного темплейта, вызывает конструктор Component и находит дочерние элементы разметки.

Поля класса:  
`protected basketButton: HTMLButtonElement` - защищенный HTML-элемент, содержащий кнопку корзины
`protected counterElement: HTMLElement` - защищенный HTML-элемент, содержащий общее количество товаров, добавленных в корзину

Методы:  
`set counter (value: number)` - устанавливает содержимое counterElement

##### Интерфейс GalleryData
Описывает данные, передаваемые в метод render каталога товаров

interface GalleryData {
 catalogue: HTMLElement[] - все товары в каталоге
}

##### Класс Gallery
Класс каталога товаров. Содержит общие элементы и методы, которые используются в каталоге товаров

Конструктор:
`constructor(container: HTMLElement)` - принимает DOM-элемент клонированного темплейта, вызывает конструктор Component и находит дочерние элементы разметки.

Поля класса:  
`protected catalogueElement: HTMLElement` - защищенный HTML-элемент, содержащий список всех товаров каталога

Методы:  
`set catalogue (items: HTMLElement[])` - устанавливает содержимое catalogueElement


#### События
##### События моделей данных
`catalogue:products-changed` - генерируется при изменении списка товаров в каталоге
`catalogue:card-changed` - генерируется при сохранении выбранного товара в модели
`cart:changed` - генерируется при любом изменении списка товаров в корзине
`buyer:data-changed` - генерируется при изменении или очистке данных покупателя

##### События представлений
`card:selected` - генерируется при нажатии на карточку товара в каталоге
`card:click` - генерируется при нажатии кнопки в карточке товара
`cart:delete` - генерируется при нажатии кнопки удаления товара из корзины
`cart:open` - генерируется при нажатии на иконку корзины в шапке
`cart:purchase` - генерируется при нажатии кнопки "Оформить" в корзине
`order:next` - генерируется при нажатии кнопки "Далее" в первой форме
`order:pay` - генерируется при нажатии кнопки "Оплатить" во второй форме
`order:changed` - генерируется при изменении данных в форме оплаты и адреса
`contacts:changed` - генерируется при изменении данных в форме контактов
`order:success` - генерируется при нажатии на кнопку "За новыми покупками!"

#### Презентер
Презентер реализован в основном скрипте приложения `main.ts` без выделения в отдельный класс. Он связывает слой данных и слой представления через событийную модель. Презентер подписывается на события моделей данных и представлений, обрабатывает их и вызывает нужные методы моделей и компонентов представления.

Presenter не генерирует события — только обрабатывает их. Перерисовка компонентов представления происходит только в двух случаях: при обработке события от модели данных об изменении данных, или при открытии модального окна.